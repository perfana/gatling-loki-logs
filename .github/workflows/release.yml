To make the GitHub Actions workflow a manual release instead of being triggered on push, you can use the `workflow_dispatch` event. Here is the updated workflow configuration:

name: Release to Maven Central

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up JDK 17
        uses: actions/setup-java@v3
        with:
          distribution: 'temurin'
          java-version: '17'

      - name: Cache SBT
        uses: actions/cache@v3
        with:
          path: |
            ~/.ivy2/cache
            ~/.sbt
            ~/.coursier
          key: ${{ runner.os }}-sbt-${{ hashFiles('**/build.sbt') }}
          restore-keys: |
            ${{ runner.os }}-sbt-

      - name: Install dependencies
        run: sbt update

      - name: Publish to Maven Central
        env:
          SONATYPE_USERNAME: ${{ secrets.SONATYPE_USERNAME }}
          SONATYPE_PASSWORD: ${{ secrets.SONATYPE_PASSWORD }}
          PGP_PASSPHRASE: ${{ secrets.GGP_SIGNING_KEY }}
          PGP_SIGNING_KEY: ${{ secrets.GGP_SIGNING_PASSWORD }}
        run: |
          echo $PGP_SIGNING_KEY | base64 --decode > /tmp/pgp-key.asc
          gpg --batch --import /tmp/pgp-key.asc
          sbt +publishSigned sonatypeBundleRelease
